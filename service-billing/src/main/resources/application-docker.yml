spring:
    datasource:
        url: jdbc:postgresql://postgresql:5432/ecommerce?currentSchema=billing_service,public
        username: postgres
        password: postgres
    kafka:
        bootstrap-servers: kafka:9092
