spring:
    datasource:
        url: jdbc:postgresql://postgresql:5432/ecommerce?currentSchema=catalog_service,public
        username: postgres
        password: postgres
    kafka:
        bootstrap-servers: kafka:9092
