spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/ecommerce?currentSchema=catalog_service,public
        username: postgres
        password: postgres
    kafka:
        bootstrap-servers: localhost:9094
